import { Layout } from '../';
import { Typography } from '@material-ui/core'

const TasksPanel = () => {
  return (
    <Layout current='tasks'><Typography>Tasks</Typography></Layout>
  )
};

export default TasksPanel;